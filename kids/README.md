# Luma Kids channel

`latest.json` in this directory is managed by the private `P-Anny/luma-kids` signed-release workflow. Luma Kids reads only this channel and must use a signing key different from adult Luma.
